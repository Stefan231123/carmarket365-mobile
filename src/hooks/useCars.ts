// Custom hook for car listing operations
// TODO: Implement with Apollo Client useQuery/useMutation

// useCars(filters) — fetch paginated car listings
// useCar(id) — fetch single car detail
// useCreateCar() — create new listing
// useUpdateCar() — update existing listing
// useDeleteCar() — delete listing
// useToggleSave(carId) — save/unsave car
// useMyCars() — fetch current user's listings
// useMySavedCars() — fetch saved cars
